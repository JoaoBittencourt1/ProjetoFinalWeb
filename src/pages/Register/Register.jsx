import RegisterContainer from '../../components/RegisterContainer/RegisterContainer';

function Register() {
    return (
        <div>
            <RegisterContainer />
        </div>
    );
}

export default Register; 